#!/bin/bash

# Check if the CFILE variable is set
if [ -z "$CFILE" ]; then
    echo "Error: CFILE variable is not set."
    exit 1
fi

# Run the C file through the preprocessor and save the result into 'c'
cpp "$CFILE" -o c

echo "Preprocessing completed. Output saved to 'c'."
